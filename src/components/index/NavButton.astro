---
const { class: className } = Astro.props;
---

<button
  class={`${className} aria-label="btn-nav" px-2 py-1 text-sm w-auto rounded-full hover:text-hub-title`}
  {...Astro.props}
>
  <slot />
</button>
