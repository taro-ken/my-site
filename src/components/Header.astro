---
const nav = [
  { href: "/projects/", label: "Portfolio" },
  { href: "/blog/",     label: "Blog" },
  { href: "/about/",    label: "About" },
  { href: "/contact/",  label: "Contact" },
];
---
<header class="sticky top-0 z-50 bg-white/80 backdrop-blur border-b">
  <div class="container h-14 flex items-center justify-between">
    <a href="/" class="font-extrabold tracking-tight no-underline text-lg">KENTARO</a>
    <nav class="flex gap-6 text-sm">
      {nav.map(n => (
        <a href={n.href} class="no-underline text-gray-700 hover:text-black">{n.label}</a>
      ))}
    </nav>
  </div>
</header>
